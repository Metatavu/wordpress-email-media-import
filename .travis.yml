language: php

notifications:
  email:
    on_success: never
    on_failure: change

cache:
  directories:
    - $HOME/.composer

services:
  - mysql

addons:
  apt:
    packages:
      - nodejs

php:
  - 5.6

env:
  - WP_VERSION=latest WP_MULTISITE=0
  
before_script:
  - bash travis/before-script.sh

script: phpunit
  
after_script:
  - bash travis/after-script.sh
